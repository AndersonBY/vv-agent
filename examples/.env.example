# S3WorkspaceBackend 配置
# 复制此文件为 .env 并填入真实值
#
# 支持 AWS S3 / MinIO / Aliyun OSS (S3 兼容模式) / Cloudflare R2 等

# S3 凭证
S3_ACCESS_KEY_ID=your-access-key-id
S3_SECRET_ACCESS_KEY=your-secret-access-key

# Bucket 名称
S3_BUCKET=my-workspace-bucket

# 对象 key 前缀 (可选, 留空则使用 bucket 根目录)
S3_PREFIX=vv-agent/workspace

# 自定义 endpoint (S3 兼容服务必填, AWS S3 可留空)
# MinIO 示例:       http://localhost:9000
# Aliyun OSS 示例:  https://oss-cn-hangzhou.aliyuncs.com
# Cloudflare R2:    https://<account_id>.r2.cloudflarestorage.com
S3_ENDPOINT_URL=

# AWS region (可选, 大多数 S3 兼容服务可忽略)
S3_REGION=

# 寻址风格: virtual (默认) 或 path
# Aliyun OSS / Cloudflare R2 / AWS S3 用 virtual
# MinIO 用 path
S3_ADDRESSING_STYLE=virtual
